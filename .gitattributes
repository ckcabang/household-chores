# Normalize line endings to LF in the repo; check out native on each OS.
* text=auto eol=lf

# Vendored third-party assets: store byte-for-byte, no normalization.
chores/static/chores/vendor/** -text
